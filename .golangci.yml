version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - revive
    - unused
    - staticcheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
formatters:
  enable:
    - gofmt
