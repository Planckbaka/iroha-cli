version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - revive
    - unused
    - gosimple
    - gofmt
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
